<h1>Dates in *Location*: Aim for unforgettable!</h1>

<ul>
  <% @dates.each do |date| %>
    <li> 
      <%= date.title %> 
      <%= date.location %>
      <%= date.description %>
      <%= link_to 'details', dates_show_path(date) %>
    </li>
  <% end %>
</ul>